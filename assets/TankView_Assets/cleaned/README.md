# Cleaned assets (output)

Transparent PNG cutouts produced by `tools/remove-backgrounds.mjs` from the
images in `../raw/`. Curate keepers into `public/assets/...` and register their
paths in `src/data/assets.ts` to use them in the game.
